The project idea is that json should be able to describe forms and ui related element. 
The project accepts a json object and based on that json object, renders the related ui(input) element to screen
