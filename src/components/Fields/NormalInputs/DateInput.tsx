import { DateTimeFields } from "../../../schema/types";
import Input, { InputProps } from "../Input";

const DateInput = (props: InputProps<DateTimeFields>) => {
    return <Input type="" />;
};

export default DateInput;
